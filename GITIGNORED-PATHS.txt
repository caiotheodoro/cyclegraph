# Cited-but-deliberately-absent paths, and why each exists.
#
# titer added this file because its cited-path gate had been passing locally for
# the wrong reason: the files existed on the machine that wrote the docs. A gate
# that only passes where the author sits is not a gate.

# Private outreach and jurisdiction material. Never leaves the machine; never
# shapes a finding. See docs/DECISIONS.md D008.
docs/private/

# Regenerated release staging, derived from results/ and docs/.
hf/dataset/README.md
hf/dataset/CONTRACTS.md

# Real credentials. .env.example is tracked; .env is not.
.env

# Pilot per-clip records and their inputs. The pilot factory is nameable, so every one of
# these carries a per-factory identifier in its clip_id and none may ever be tracked
# (docs/DECISIONS.md D018, docs/ETHICS.md). The gate on results/ would catch it; this
# declares them so the cited-path gate does not fail on a promise the docs make.
results/pilot/detections.jsonl
results/pilot/labels.jsonl
results/pilot/frame_signal.jsonl
results/pilot/hand_speed.jsonl

# The Space's payload, built from results/ by scripts/export_space_data.py. Derived, so it is
# not tracked; space/index.html fetches it at runtime and the docs cite it.
space/data.json
