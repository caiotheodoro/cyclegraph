"""`MeasurementCard`: generated, never written.

`docs/ARCHITECTURE.md`: *"a hand-edited card is a transcribed number, and transcribed numbers
are how a claim quietly stops being true."* Nothing in this package accepts a claim's value
from a human; every one is read from a file under `results/`.

Deliberately empty of re-exports, for the reason `cyclegraph.corpus` gives.
"""
