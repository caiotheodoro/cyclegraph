# Paths referenced in the docs that do not exist yet, and the wave that creates them.
# The cited-path gate checks every other path resolves; this file makes the promises
# explicit so a typo still fails the build. Convention inherited from titer.

# W2 -- models (see below); W3 -- corpus access and the signal

# W2 -- contracts in code

# W5 -- cycles

# W7 -- exposure

# W7/W8 -- estimation and the card
results/bootstrap_replicates.json

# W9 -- release. Every path this section promised has landed; the section stays as the
# place a future release path is declared before it is written.
